services:
  c9manager-nextjs:
    container_name: c9manager-nextjs
    build: .
    restart: unless-stopped
    user: "1000:1000"
    ports:
      - "3222:3222"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - NODE_ENV=production
